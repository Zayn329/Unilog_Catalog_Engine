import { SpecForgeDashboard } from "@/components/SpecForgeDashboard";

export const metadata = {
  title: "SpecForge Master Engineering Workbench | Unilog Catalog Engine",
  description:
    "Parametric 3D CAD modeling, deterministic physics stress simulation, and evidence-first catalog validation.",
};

export default function DashboardPage() {
  return <SpecForgeDashboard />;
}
